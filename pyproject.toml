[project]
name = "cr-agent-training"
version = "0.1.0"
description = "Python training utilities for CR-Agent AgenticRL."
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.1.0",
    "datasets>=3.0.0",
    "peft>=0.13.0",
    "torch>=2.5.0",
    "transformers>=4.46.0",
]

[tool.uv]
package = false
